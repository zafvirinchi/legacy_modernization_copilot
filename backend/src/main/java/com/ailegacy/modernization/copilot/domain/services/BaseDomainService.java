package com.ailegacy.modernization.copilot.domain.services;

/**
 * Base domain service interface.
 * 
 * Domain services encapsulate business logic that doesn't naturally belong to a single entity.
 * 
 * Examples:
 * - LegacyPatternDetectionService: detects legacy architectural patterns
 * - SecurityAnalysisService: identifies security issues
 * - ModernizationRecommendationService: generates migration recommendations
 */
public interface BaseDomainService {

    // Base interface for all domain services

}
