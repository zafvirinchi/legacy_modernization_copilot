import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Reports | AI Legacy Modernization Copilot',
  description: 'View and download modernization reports',
};

export default function ReportsPage() {
  return <div>{/* Reports page content placeholder */}</div>;
}
