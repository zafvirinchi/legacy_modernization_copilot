import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'History | AI Legacy Modernization Copilot',
  description: 'View scan and analysis history',
};

export default function HistoryPage() {
  return <div>{/* History page content placeholder */}</div>;
}
