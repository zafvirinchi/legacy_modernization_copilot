import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Upload | AI Legacy Modernization Copilot',
  description: 'Upload legacy project artifacts for analysis',
};

export default function UploadPage() {
  return <div>{/* Upload page content placeholder */}</div>;
}
